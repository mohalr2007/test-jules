# Indicates that the project uses AndroidX libraries.
android.useAndroidX=true
# Automatically converts third-party libraries to be AndroidX-compatible.
android.enableJetifier=true
# Suppress the warning about using a compileSdk version not officially supported by the current AGP version.
android.suppressUnsupportedCompileSdk=34